@model Trip
@{ 
    ViewData["Title"] = "Add Trip - Page 3";
    TempData["CurrentPage"] = 3;
}
@if (TempData.Peek("Destination") != null)
{
    @section SubHead
    {
        <h2>Add Things To Do in @TempData.Peek("Destination").ToString()</h2>
    }
}

<form asp-action="Add" method="post">
    <div class="form-group">
        <input asp-for="ThingToDo1" />
    </div>
    <div class="form-group">
        <input asp-for="ThingToDo2" />
    </div>
    <div class="form-group">
        <input asp-for="ThingToDo3" />
    </div>
    <button class="btn btn-primary" type="submit">Save</button>
    <a asp-action="Cancel" class="btn btn-primary">Cancel</a>
</form>